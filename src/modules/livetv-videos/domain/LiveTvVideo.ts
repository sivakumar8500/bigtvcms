export interface VideoTag {
  id: string;
  name: string;
  slug: string;
  createdAt: string;
  updatedAt: string;
}

export interface TagVideo {
  fileName: string;
  sizeBytes: number;
  createdAt: string;
  url: string;
}
